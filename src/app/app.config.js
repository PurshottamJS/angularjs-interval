;
(function(window, angular, undefined) {
    'use strict';
    angular
        .module('app')
        .config(config);
    config.$inject = [];

    function config() {}
}(window, angular, undefined));
